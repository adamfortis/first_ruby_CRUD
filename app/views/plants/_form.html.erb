<%= form_for(@plant) do |f| %>
  <% if @plant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plant.errors.count, "error") %> prohibited this plant from being saved:</h2>

      <ul>
      <% @plant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Plant Name" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br>
    <%= f.select :person_id, Person.all.collect{ |p|[ p.name, p.id ] },
      include_blank: true, selected: params[:person_id] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
